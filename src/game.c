#include "./include/game.h"
#include "./include/raylib.h"


void GameLoop(void) {
	BeginDrawing();
	ClearBackground(WHITE);

	EndDrawing();
}

void GameoverLoop(void) {
	BeginDrawing();
	ClearBackground(WHITE);

	EndDrawing();
}
